# Automatically added by dh_installinit
if ([ -x "/etc/init.d/ovn-controller-vtep" ] || [ -e "/etc/init/ovn-controller-vtep.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d ovn-controller-vtep stop || exit $?
fi
# End automatically added section
