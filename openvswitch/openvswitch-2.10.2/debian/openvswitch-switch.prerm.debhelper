# Automatically added by dh_installinit
if ([ -x "/etc/init.d/openvswitch-switch" ] || [ -e "/etc/init/openvswitch-switch.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d openvswitch-switch stop || exit $?
fi
# End automatically added section
