# Automatically added by dh_installinit
if ([ -x "/etc/init.d/ovn-central" ] || [ -e "/etc/init/ovn-central.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d ovn-central stop || exit $?
fi
# End automatically added section
