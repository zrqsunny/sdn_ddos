# Automatically added by dh_installinit
if ([ -x "/etc/init.d/openvswitch-ipsec" ] || [ -e "/etc/init/openvswitch-ipsec.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d openvswitch-ipsec stop || exit $?
fi
# End automatically added section
